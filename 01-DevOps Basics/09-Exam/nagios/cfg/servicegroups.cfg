define servicegroup {
    servicegroup_name       service-ping
    alias                   Hosts ping services
    members                 localhost, PING, avm.dob.exam, PING, dvm.dob.exam, PING, jvm.dob.exam, PING
}

define servicegroup {
    servicegroup_name       CONT
    alias                   Docker container services
    members                 dvm.dob.exam, DOB-PRODUCER, dvm.dob.exam, DOB-CONSUMER, dvm.dob.exam, DOB-STORAGE
}
